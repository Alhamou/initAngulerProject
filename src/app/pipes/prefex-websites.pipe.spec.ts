import { PrefexWebsitesPipe } from './prefex-websites.pipe';

describe('PrefexWebsitesPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefexWebsitesPipe();
    expect(pipe).toBeTruthy();
  });
});
